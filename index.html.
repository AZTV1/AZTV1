<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AZTV1 - مكتبة الأفلام</title>
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-database.js"></script>
    <style>
        body { background-color: #000; color: #fff; font-family: sans-serif; margin: 0; text-align: center; }
        header { padding: 25px; border-bottom: 2px solid #ff0000; background: #050505; color: #FFD700; font-size: 30px; font-weight: bold; text-shadow: 0 0 10px rgba(255, 215, 0, 0.3); }
        .add-section { background: #0a0a0a; padding: 20px; margin: 20px auto; max-width: 450px; border: 1px solid #333; border-radius: 12px; box-shadow: 0 5px 15px rgba(255,0,0,0.1); }
        input { width: 85%; padding: 12px; margin: 10px 0; border-radius: 8px; border: 1px solid #444; background: #1a1a1a; color: #fff; text-align: center; }
        .btn-add { background: #ff0000; color: #fff; padding: 12px; border: none; border-radius: 8px; cursor: pointer; font-weight: bold; width: 90%; font-size: 16px; transition: 0.3s; }
        .btn-add:hover { background: #cc0000; }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; padding: 20px; }
        .movie-card { background: #111; border: 1px solid #222; border-radius: 12px; padding: 15px; transition: 0.3s; }
        .movie-card:hover { border-color: #FFD700; }
        video { width: 100%; border-radius: 8px; background: #000; }
    </style>
</head>
<body>
    <header>AZTV1</header>
    
    <div class="add-section">
        <h3 style="color: #ff0000;">إضافة فيلم جديد للمكتبة</h3>
        <input type="text" id="movieName" placeholder="اكتب اسم الفيلم">
        <input type="text" id="movieUrl" placeholder="ضع رابط الفيديو المباشر">
        <button class="btn-add" onclick="saveMovie()">إضافة الآن ✅</button>
    </div>

    <div class="grid" id="moviesContainer"></div>

    <script>
        // الربط مع البرنامج الثاني (Firebase)
        const firebaseConfig = {
            databaseURL: "https://mymoveis-default-rtdb.firebaseio.com/" 
        };
        firebase.initializeApp(firebaseConfig);
        const db = firebase.database();

        // حفظ البيانات
        function saveMovie() {
            const name = document.getElementById('movieName').value;
            const url = document.getElementById('movieUrl').value;
            if (name && url) {
                db.ref('movies').push({ name: name, url: url }).then(() => {
                    alert("تم حفظ الفيلم بنجاح! 🎉");
                    document.getElementById('movieName').value = "";
                    document.getElementById('movieUrl').value = "";
                });
            } else { alert("عبي الخانات أولاً!"); }
        }

        // عرض الأفلام تلقائياً
        db.ref('movies').on('value', (snap) => {
            const container = document.getElementById('moviesContainer');
            container.innerHTML = ""; 
            snap.forEach((child) => {
                const data = child.val();
                container.innerHTML += `
                    <div class="movie-card">
                        <p style="color:#FFD700; font-weight:bold; margin-bottom:10px;">${data.name}</p>
                        <video controls src="${data.url}"></video>
                    </div>`;
            });
        });
    </script>
</body>
</html>
